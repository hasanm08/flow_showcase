name: flow_showcase
description: >-
  A lightweight, high-performance Flutter package for interactive onboarding
  walkthroughs with spotlight overlays, step indicators, and declarative targets.
version: 1.0.1
homepage: https://github.com/hasanm08/flow_showcase
repository: https://github.com/hasanm08/flow_showcase
issue_tracker: https://github.com/hasanm08/flow_showcase/issues

environment:
  sdk: ^3.5.0
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
